{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: test:watch",
            "request": "launch",
            "command": "npm run test:watch",
            "cwd": "${workspaceFolder}"
        }
    ]
}